# Concepts
	* Repositories
	* Commits
	* Branches
	* Merging
	* Tags
  
 git init # initialize the git - a repository
 git config --global user.name "Your Name"  --> first commit
 git config --global user.email "Your e-mail" --> first commit, by command line